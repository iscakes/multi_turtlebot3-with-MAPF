// Auto-generated. Do not edit!

// (in-package robint_msgs.msg)


"use strict";

const _serializer = _ros_msg_utils.Serialize;
const _arraySerializer = _serializer.Array;
const _deserializer = _ros_msg_utils.Deserialize;
const _arrayDeserializer = _deserializer.Array;
const _finder = _ros_msg_utils.Find;
const _getByteLength = _ros_msg_utils.getByteLength;
let geometry_msgs = _finder('geometry_msgs');

//-----------------------------------------------------------

class MoveBaseGoal {
  constructor(initObj={}) {
    if (initObj === null) {
      // initObj === null is a special case for deserialization where we don't initialize fields
      this.target_pose = null;
      this.goal_type = null;
      this.enable_goal_candidate_search = null;
      this.enable_goal_increase_tolerance = null;
      this.enable_fixed_straight_path = null;
    }
    else {
      if (initObj.hasOwnProperty('target_pose')) {
        this.target_pose = initObj.target_pose
      }
      else {
        this.target_pose = new geometry_msgs.msg.PoseStamped();
      }
      if (initObj.hasOwnProperty('goal_type')) {
        this.goal_type = initObj.goal_type
      }
      else {
        this.goal_type = '';
      }
      if (initObj.hasOwnProperty('enable_goal_candidate_search')) {
        this.enable_goal_candidate_search = initObj.enable_goal_candidate_search
      }
      else {
        this.enable_goal_candidate_search = false;
      }
      if (initObj.hasOwnProperty('enable_goal_increase_tolerance')) {
        this.enable_goal_increase_tolerance = initObj.enable_goal_increase_tolerance
      }
      else {
        this.enable_goal_increase_tolerance = false;
      }
      if (initObj.hasOwnProperty('enable_fixed_straight_path')) {
        this.enable_fixed_straight_path = initObj.enable_fixed_straight_path
      }
      else {
        this.enable_fixed_straight_path = false;
      }
    }
  }

  static serialize(obj, buffer, bufferOffset) {
    // Serializes a message object of type MoveBaseGoal
    // Serialize message field [target_pose]
    bufferOffset = geometry_msgs.msg.PoseStamped.serialize(obj.target_pose, buffer, bufferOffset);
    // Serialize message field [goal_type]
    bufferOffset = _serializer.string(obj.goal_type, buffer, bufferOffset);
    // Serialize message field [enable_goal_candidate_search]
    bufferOffset = _serializer.bool(obj.enable_goal_candidate_search, buffer, bufferOffset);
    // Serialize message field [enable_goal_increase_tolerance]
    bufferOffset = _serializer.bool(obj.enable_goal_increase_tolerance, buffer, bufferOffset);
    // Serialize message field [enable_fixed_straight_path]
    bufferOffset = _serializer.bool(obj.enable_fixed_straight_path, buffer, bufferOffset);
    return bufferOffset;
  }

  static deserialize(buffer, bufferOffset=[0]) {
    //deserializes a message object of type MoveBaseGoal
    let len;
    let data = new MoveBaseGoal(null);
    // Deserialize message field [target_pose]
    data.target_pose = geometry_msgs.msg.PoseStamped.deserialize(buffer, bufferOffset);
    // Deserialize message field [goal_type]
    data.goal_type = _deserializer.string(buffer, bufferOffset);
    // Deserialize message field [enable_goal_candidate_search]
    data.enable_goal_candidate_search = _deserializer.bool(buffer, bufferOffset);
    // Deserialize message field [enable_goal_increase_tolerance]
    data.enable_goal_increase_tolerance = _deserializer.bool(buffer, bufferOffset);
    // Deserialize message field [enable_fixed_straight_path]
    data.enable_fixed_straight_path = _deserializer.bool(buffer, bufferOffset);
    return data;
  }

  static getMessageSize(object) {
    let length = 0;
    length += geometry_msgs.msg.PoseStamped.getMessageSize(object.target_pose);
    length += _getByteLength(object.goal_type);
    return length + 7;
  }

  static datatype() {
    // Returns string type for a message object
    return 'robint_msgs/MoveBaseGoal';
  }

  static md5sum() {
    //Returns md5sum for a message object
    return 'fb8462c79e86387a5e3c986ae0b122df';
  }

  static messageDefinition() {
    // Returns full string definition for message
    return `
    # ====== DO NOT MODIFY! AUTOGENERATED FROM AN ACTION DEFINITION ======
    geometry_msgs/PoseStamped target_pose
    string goal_type
    bool enable_goal_candidate_search
    bool enable_goal_increase_tolerance
    bool enable_fixed_straight_path
    
    ================================================================================
    MSG: geometry_msgs/PoseStamped
    # A Pose with reference coordinate frame and timestamp
    Header header
    Pose pose
    
    ================================================================================
    MSG: std_msgs/Header
    # Standard metadata for higher-level stamped data types.
    # This is generally used to communicate timestamped data 
    # in a particular coordinate frame.
    # 
    # sequence ID: consecutively increasing ID 
    uint32 seq
    #Two-integer timestamp that is expressed as:
    # * stamp.sec: seconds (stamp_secs) since epoch (in Python the variable is called 'secs')
    # * stamp.nsec: nanoseconds since stamp_secs (in Python the variable is called 'nsecs')
    # time-handling sugar is provided by the client library
    time stamp
    #Frame this data is associated with
    string frame_id
    
    ================================================================================
    MSG: geometry_msgs/Pose
    # A representation of pose in free space, composed of position and orientation. 
    Point position
    Quaternion orientation
    
    ================================================================================
    MSG: geometry_msgs/Point
    # This contains the position of a point in free space
    float64 x
    float64 y
    float64 z
    
    ================================================================================
    MSG: geometry_msgs/Quaternion
    # This represents an orientation in free space in quaternion form.
    
    float64 x
    float64 y
    float64 z
    float64 w
    
    `;
  }

  static Resolve(msg) {
    // deep-construct a valid message object instance of whatever was passed in
    if (typeof msg !== 'object' || msg === null) {
      msg = {};
    }
    const resolved = new MoveBaseGoal(null);
    if (msg.target_pose !== undefined) {
      resolved.target_pose = geometry_msgs.msg.PoseStamped.Resolve(msg.target_pose)
    }
    else {
      resolved.target_pose = new geometry_msgs.msg.PoseStamped()
    }

    if (msg.goal_type !== undefined) {
      resolved.goal_type = msg.goal_type;
    }
    else {
      resolved.goal_type = ''
    }

    if (msg.enable_goal_candidate_search !== undefined) {
      resolved.enable_goal_candidate_search = msg.enable_goal_candidate_search;
    }
    else {
      resolved.enable_goal_candidate_search = false
    }

    if (msg.enable_goal_increase_tolerance !== undefined) {
      resolved.enable_goal_increase_tolerance = msg.enable_goal_increase_tolerance;
    }
    else {
      resolved.enable_goal_increase_tolerance = false
    }

    if (msg.enable_fixed_straight_path !== undefined) {
      resolved.enable_fixed_straight_path = msg.enable_fixed_straight_path;
    }
    else {
      resolved.enable_fixed_straight_path = false
    }

    return resolved;
    }
};

module.exports = MoveBaseGoal;
